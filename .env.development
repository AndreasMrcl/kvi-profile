# Dikosongkan dengan sengaja: request jadi relatif (mis. /api/auth/me)
# sehingga ditangani proxy Vite (vite.config.js) menuju http://127.0.0.1:8000.
# Hasilnya same-origin di browser -> tidak ada masalah CORS saat dev lokal.
VITE_API_BASE_URL=
